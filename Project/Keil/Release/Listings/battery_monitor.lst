C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE BATTERY_MONITOR
OBJECT MODULE PLACED IN .\Release\Objects\battery_monitor.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\battery_monitor.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECT
                    -OR(0X000C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\driver_on_chip) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Releas
                    -e\Listings\battery_monitor.lst) OBJECT(.\Release\Objects\battery_monitor.obj)

line level    source

   1          #include "battery_monitor.h"
   2          #include "user_include.h"
   3          
   4          // volatile u8 is_send_low_battery_enable = 0;
   5          
   6          // 是否发送了低电量报警（ REVIEW 在第一次上电、低功耗唤醒之后、有充电之后�
             -�需要清零）
   7          volatile u8 is_sent_low_bat_alert = 0;
   8          volatile u8 is_turn_off_by_low_bat = 0; // 是否低电量关机（ REVIEW 从低功耗唤醒，有充电�
             -��后，都清除一下该标志位）
   9          
  10          // ====================================================================
  11          static volatile u8 is_bat_vol_buff_add_enable = 0;     // 是否允许往电池电压数组中放入数据
  12          static volatile u8 is_bat_vol_buff_get_avg_enable = 0; // 是否允许往电池电压数组中获取平均
             -值
  13          
  14          // 控制一段时间内采集电池电压峰值（电压最大值）
  15          static volatile bat_vol_update_sta_t bat_vol_update_sta = BAT_VOL_UPDATE_STA_IDLE;
  16          static volatile u16 bat_vol_update_cnt = 0;
  17          
  18          // 记录一段时间内采集到的电压值：
  19          static volatile u16 bat_vol_history_buff[VOLTAGE_HISTORY_SIZE] = {0};
  20          static volatile u8 bat_vol_history_buff_idx = 0;
  21          // ====================================================================
  22          
  23          // 只在采集使用：
  24          static volatile u16 voltage_mv = 0;     //
  25          static volatile u16 max_voltage_mv = 0; // 存放一段时间内采集到的最大电压值（只在采集
             -使用，不能作为最终的判断使用）
  26          
  27          // 最后得到的、稳定的电池电压
  28          volatile u16 avg_voltage_mv = 0; // @ATTENTION 在当前文件外调用时，慎用
  29          
  30          static volatile u8 percent = 0; // 电池电量百分比
  31          // static volatile u8 last_percent = 0; // 记录上一次采集到的电量百分比，用来控制电量
             -百分比更新，充电时百分比不能下降，放电时百分比不能上升
  32          
  33          // 最终得到的、稳定的电池电量百分比
  34          volatile u8 bat_percent = 0;
  35          
  36          // 电池电量低时，每隔 xx 时间，给蓝牙ic发送一次提示，单位：ms
  37          #define SEND_LOW_BAT_TIME_PERIOD ((u32)120 * 1000)
  38          // #define SEND_LOW_BAT_TIME_PERIOD ((u32)10 * 1000)         // @USER_TO_DO  测试使用
  39          static volatile u8 is_send_low_bat_repeatedly_enable = 0; // 是否重复发送低电量报警
  40          
  41          // 电池电量百分比更新的时间计数器
  42          static volatile u16 bat_percent_update_time_cnt = 0;
  43          
  44          void bat_percent_update_time_add(void)
  45          {
  46   1          if (bat_percent_update_time_cnt < ((u16)-1))
  47   1          {
  48   2              bat_percent_update_time_cnt++;
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 2   

  49   2          }
  50   1      }
  51          
  52          // 重置电池电量百分比的更新时间
  53          void bat_percent_update_time_reset(void)
  54          {
  55   1          bat_percent_update_time_cnt = 0;
  56   1      }
  57          
  58          // 确认电池电量百分比的更新时间是否到来
  59          u8 bat_percent_update_time_is_comes(void)
  60          {
  61   1          if (bat_percent_update_time_cnt >= BATTERY_PERCENT_UPDATE_PERIOD)
  62   1          {
  63   2              return 1;
  64   2          }
  65   1          else
  66   1          {
  67   2              return 0;
  68   2          }
  69   1      }
  70          
  71          // 判断当前电压是否允许重新打开蓝牙
  72          u8 battery_bluetooth_enable_is_allowed(void)
  73          {
  74   1          return (avg_voltage_mv > BATTERY_VOLTAGE_TURN_OFF_BLUETOOTH);
  75   1      }
  76          
  77          // 控制发送低电量的周期
  78          void send_low_bat_timer_callback(void)
  79          {
  80   1          static volatile u32 cnt = 0;
  81   1          /*
  82   1             刚打开蓝牙，发送低电量报警，
  83   1             需要隔一时间再发送，又不能隔太长时间，否则蓝牙会自动关机
  84   1             这里控制刚打开蓝牙之后，发送低电量报警
  85   1          */
  86   1          static volatile u8 is_first_time_to_send = 0;
  87   1      
  88   1          if (is_sent_low_bat_alert)
  89   1          {
  90   2              cnt++;
  91   2              if ((is_first_time_to_send && cnt >= (u16)3 * 1000) ||
  92   2                  (cnt >= SEND_LOW_BAT_TIME_PERIOD))
  93   2              {
  94   3                  if (is_first_time_to_send)
  95   3                  {
  96   4                      is_first_time_to_send = 0;
  97   4                  }
  98   3      
  99   3                  cnt = 0;
 100   3                  is_send_low_bat_repeatedly_enable = 1;
 101   3              }
 102   2          }
 103   1          else
 104   1          {
 105   2              cnt = 0;
 106   2      
 107   2              // 低电量报警标志没有使能，给标志位置一
 108   2              is_first_time_to_send = 1;
 109   2          }
 110   1      }
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 3   

 111          
 112          // 由定时器调用，控制一段时间内，往数组中放入数据的时间
 113          void bat_vol_buff_add_timer_callback(void)
 114          {
 115   1          static u32 cnt = 0;
 116   1          cnt++;
 117   1          if (cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD_IN_BUFFER)
 118   1          {
 119   2              cnt = 0;
 120   2              is_bat_vol_buff_add_enable = 1;
 121   2          }
 122   1      }
 123          
 124          // 由定时器调用，控制一段时间内，从数组中获取平均值的时间
 125          void bat_vol_buff_get_avg_timer_callback(void)
 126          {
 127   1          static u32 cnt = 0;
 128   1          cnt++;
 129   1          if (cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD_IN_BUFFER_EXTRACT)
 130   1          {
 131   2              cnt = 0;
 132   2              is_bat_vol_buff_get_avg_enable = 1;
 133   2          }
 134   1      }
 135          
 136          // 由定时器调用，控制一段时间，获取电池电压的最大值（测试发现在充电和放�
             -��时，一段时间内电池电压的最大值是最接近电池电压实际值的）
 137          void bat_vol_update_timer_callback(void)
 138          {
 139   1          if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_IDLE)
 140   1          {
 141   2              bat_vol_update_cnt = 0;
 142   2              bat_vol_update_sta = BAT_VOL_UPDATE_STA_CAPTURING;
 143   2          }
 144   1          else if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_COMPLETED)
 145   1          {
 146   2              // 已经采集完成，直接返回
 147   2              return;
 148   2          }
 149   1      
 150   1          bat_vol_update_cnt++;
 151   1          if (bat_vol_update_cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD)
 152   1          {
 153   2              /*
 154   2                  这里可以不用清零，只有 bat_vol_update_sta
 155   2                  刚进入 BAT_VOL_UPDATE_STA_IDLE 时清零
 156   2              */
 157   2              // bat_vol_update_cnt = 0;
 158   2              bat_vol_update_sta = BAT_VOL_UPDATE_STA_COMPLETED;
 159   2          }
 160   1      }
 161          
 162          // 根据ad值，转换成对应的电池电压值(单位：mV)
 163          u16 get_battery_voltage_by_adc(u16 adc_val)
 164          {
 165   1          u16 voltage_mv = ADC_TO_BATTERY_VOLTAGE_MV(adc_val);
 166   1          // 打印转换好的电压值（发现实际打印的电压比万用表量出的电压大了0.13V）
 167   1      
 168   1      #if 0
                  // printf("voltage_mv == %u\n", voltage_mv);
                  if (voltage_mv > 130)
                  {
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 4   

                      // 这里做电压补偿（实际打印的电压比万用表量出的电压大了0.13V，需要减
             -去0.13V）
                      voltage_mv -= 130;
                  }
                  // printf("voltage_mv == %u\n", voltage_mv);
              #endif
 177   1      
 178   1      #if 0
                  if (is_in_charging)
                  {
                      // 充电时，减去 0.1 V，作为补偿。如果电压接近 4.2V 时，不用补偿
                      if ((voltage_mv <= 4000) && voltage_mv > 100)
                      {
                          voltage_mv -= 100;
                      }
                  }
              #endif
 188   1      
 189   1          return voltage_mv;
 190   1      }
 191          
 192          // void send_low_battery_timer_callback(void)
 193          // {
 194          //     static u32 cnt = 0;
 195          //     cnt++;
 196          //     if (cnt >= (u32)30 * 1000)
 197          //     {
 198          //         is_send_low_battery_enable = 1;
 199          //         cnt = 0;
 200          //     }
 201          // }
 202          
 203          // 根据电压值计算初始电量百分比 (线性估算)
 204          u8 get_battery_percentage_by_voltage(u16 voltage_mv)
 205          {
 206   1          u32 percentage;
 207   1          if (voltage_mv >= BATTERY_FULL_VOLTAGE)
 208   1          {
 209   2              return 100;
 210   2          }
 211   1          else if (voltage_mv <= BATTERY_VOLTAGE_TURN_OFF_LOW_POWER)
 212   1          {
 213   2              return 0;
 214   2          }
 215   1          else if (voltage_mv >= BATTERY_VOLTAGE_TURN_OFF_LOW_POWER &&
 216   1                   voltage_mv <= (BATTERY_LOW_WARNING_VOLTAGE + 100))
 217   1          {
 218   2              /*
 219   2                  大于 空电电压  xx V ， 小于 (低电量提示电压+100mV) == xx V
 220   2                  映射到 0-25% 的电量百分比
 221   2              */ 
 222   2              percentage = ((u32)(voltage_mv - BATTERY_VOLTAGE_TURN_OFF_LOW_POWER) * 25) /
 223   2                           ((u16)BATTERY_LOW_WARNING_VOLTAGE + 100 - BATTERY_VOLTAGE_TURN_OFF_LOW_POWER);
 224   2              return (u8)percentage;
 225   2          }
 226   1      
 227   1          // 线性插值计算百分比
 228   1          percentage = ((u32)(voltage_mv - BATTERY_VOLTAGE_TURN_OFF_LOW_POWER) * 100) /
 229   1                       (BATTERY_FULL_VOLTAGE - BATTERY_VOLTAGE_TURN_OFF_LOW_POWER);
 230   1      
 231   1          return (u8)percentage > 100 ? 100 : (u8)percentage;
 232   1      }
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 5   

 233          
 234          void bat_vol_history_buff_init(u16 voltage_mv)
 235          {
 236   1          u8 i;
 237   1          for (i = 0; i < VOLTAGE_HISTORY_SIZE; i++)
 238   1          {
 239   2              bat_vol_history_buff[i] = voltage_mv;
 240   2          }
 241   1          bat_vol_history_buff_idx = 0;
 242   1      }
 243          
 244          void bat_vol_history_buff_add(u16 voltage_mv)
 245          {
 246   1          bat_vol_history_buff[bat_vol_history_buff_idx] = voltage_mv;
 247   1          bat_vol_history_buff_idx = (bat_vol_history_buff_idx + 1) % VOLTAGE_HISTORY_SIZE;
 248   1          // bat_vol_history_buff_idx++;
 249   1          // if (bat_vol_history_buff_idx >= VOLTAGE_HISTORY_SIZE)
 250   1          // {
 251   1          //     bat_vol_history_buff_idx = 0;
 252   1          // }
 253   1      }
 254          
 255          // 获取缓冲区中所有元素的平均值
 256          u16 bat_vol_history_buff_get_avg(void)
 257          {
 258   1          u8 i;
 259   1          u32 ret = 0;
 260   1          for (i = 0; i < VOLTAGE_HISTORY_SIZE; i++)
 261   1          {
 262   2              ret += bat_vol_history_buff[i];
 263   2          }
 264   1          return (u16)(ret / VOLTAGE_HISTORY_SIZE);
 265   1      }
 266          
 267          #if 0
              // 在低功耗唤醒后调用，由唤醒后第一次采集到的ad值来初始化相关数据
              // void battery_monitor_init_by_adc_val(u16 adc_val)
              // 强制刷新
              void battery_monitor_refresh_by_adc_val(u16 adc_val)
              {
                  voltage_mv = get_battery_voltage_by_adc(adc_val);
                  bat_vol_history_buff_init(voltage_mv);
                  max_voltage_mv = voltage_mv;
                  avg_voltage_mv = voltage_mv;
                  percent = get_battery_percentage_by_voltage(voltage_mv);
                  last_percent = percent;
                  bat_percent = percent;
                  bat_vol_update_sta = BAT_VOL_UPDATE_STA_COMPLETED;
              }
              #endif
 283          
 284          #if USER_DEBUG_ENABLE
              
              #if 0
              // 测试用，手动改变得到的电池电压
              void user_test_init_by_voltage_mv(u16 test_voltage_mv)
              {
                  // printf("test_voltage_mv == %u\n", test_voltage_mv);
                  voltage_mv = test_voltage_mv;
                  bat_vol_history_buff_init(voltage_mv);
                  max_voltage_mv = voltage_mv;
                  avg_voltage_mv = voltage_mv;
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 6   

                  percent = get_battery_percentage_by_voltage(voltage_mv);
                  last_percent = percent;
                  bat_percent = percent;
                  bat_vol_update_sta = BAT_VOL_UPDATE_STA_COMPLETED;
              
                  // printf("voltage_mv == %u\n", voltage_mv);
                  // printf("max_voltage_mv == %u\n", max_voltage_mv);
                  // printf("avg_voltage_mv == %u\n", voltage_mv);
                  // printf("percent == %u\n", (u16)percent);
                  // printf("last_percent == %u\n", (u16)last_percent);
                  // printf("bat_percent == %u\n", (u16)bat_percent);
              }
              #endif
              
              #endif
 310          
 311          /**
 312           * @brief 更新电池电压，由定时器调用。函数内部会更新电池电压和电池电量百分
             -比相关的变量
 313           *
 314           */
 315          void battery_voltage_update_by_isr(void)
 316          {
 317   1          volatile u16 adc_val = 0;
 318   1          volatile u16 cur_voltage_mv = 0; // 存放当前采集到的电压值
 319   1      
 320   1          // 获取AD值（ad值有更新才获取）
 321   1          if (adc_get_update_flag(ADC_CHANNEL_SEL_BAT_DET))
 322   1          {
 323   2              adc_clear_update_flag(ADC_CHANNEL_SEL_BAT_DET);
 324   2              adc_val = adc_get_val(ADC_CHANNEL_SEL_BAT_DET);
 325   2              // printf("bat adc val == %u\n", adc_val);
 326   2              cur_voltage_mv = get_battery_voltage_by_adc(adc_val);
 327   2              // printf("cur_voltage_mv == %u\n", cur_voltage_mv);
 328   2      
 329   2              if (0 == voltage_mv)
 330   2              {
 331   3                  // 如果还没有采集过电池电压，直接将第一次采集到的ad值转换成电池�
             -�压
 332   3                  voltage_mv = cur_voltage_mv;
 333   3      
 334   3                  bat_vol_history_buff_init(voltage_mv);
 335   3                  // avg_voltage_mv = bat_vol_history_buff_get_avg();
 336   3                  avg_voltage_mv = cur_voltage_mv;
 337   3                  percent = get_battery_percentage_by_voltage(avg_voltage_mv);
 338   3                  // last_percent = percent;
 339   3                  bat_percent = percent; // 初始化全局变量，电池电量百分比
 340   3      
 341   3      #if USER_DEBUG_ENABLE
                          printf("bat monitor init\n");
                          // printf("bat monitor init\n");
                          printf("avg_voltage_mv == %u\n", avg_voltage_mv);
                          // printf("percent == %u\n", (u16)percent);
                          printf("bat percent == %u\n", (u16)bat_percent);
              #endif
 348   3              }
 349   2      
 350   2              // 采集一段时间的电压值，取其中最大的值作为电池电压
 351   2              /*
 352   2                  @attention
 353   2                  测试时，如果需要改变电池电压和电量百分比，需要屏蔽下面这段程序�
             -��
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 7   

 354   2                  否则会更新 voltage_mv 和 max_voltage_mv，可能会被赋值为0，
 355   2                  导致又进入了 电池电量相关变量的初始化
 356   2              */
 357   2              /*
 358   2                  @attention 这里没有判断上一次的电压值，会实时更新 voltage_mv
 359   2                  在充电时，哪怕电压有下降，也会更新 voltage_mv
 360   2                  在放电时，电压有上升，也会更新 voltage_mv
 361   2              */
 362   2              if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_COMPLETED)
 363   2              {
 364   3                  // 如果已经采集了一段时间的电压值，则将 voltage_mv 赋值为 max_voltage_mv
 365   3                  voltage_mv = max_voltage_mv;
 366   3      
 367   3                  max_voltage_mv = 0; // 清零，准备下一轮采集
 368   3                  bat_vol_update_sta = BAT_VOL_UPDATE_STA_IDLE;
 369   3      
 370   3                  // 输出计算结果 (调试用)
 371   3                  // printf("Voltage: %umV\n", voltage_mv);
 372   3              }
 373   2              else if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_CAPTURING)
 374   2              {
 375   3                  // 如果还在采集时间内，更新采集到的最大电压值 max_voltage_mv
 376   3                  if (max_voltage_mv < cur_voltage_mv)
 377   3                  {
 378   4                      max_voltage_mv = cur_voltage_mv;
 379   4                  }
 380   3              }
 381   2          }
 382   1      }
 383          
 384          // 修改后的电池监控处理函数
 385          void battery_monitor_handle(void)
 386          {
 387   1          // volatile u16 adc_val = 0;
 388   1          // volatile u16 cur_voltage_mv = 0; // 存放当前采集到的电压值
 389   1      
 390   1          // 测试时使用：
 391   1          // if (cur_voltage_mv != 0)
 392   1          // {
 393   1          //     printf("cur_voltage_mv == %u\n", cur_voltage_mv);
 394   1          // }
 395   1      
 396   1          if (voltage_mv == 0)
 397   1          {
 398   2              return; // 尚未获取到电压值，不执行以下操作
 399   2          }
 400   1      
 401   1          // 每隔一段时间，将采集到的电压值放入数组
 402   1          if (is_bat_vol_buff_add_enable)
 403   1          {
 404   2              bat_vol_history_buff_add(voltage_mv);
 405   2              is_bat_vol_buff_add_enable = 0;
 406   2          }
 407   1      
 408   1          // 每隔一段时间，将数组中的数据进行平均
 409   1          if (is_bat_vol_buff_get_avg_enable)
 410   1          {
 411   2              avg_voltage_mv = bat_vol_history_buff_get_avg();
 412   2      #if USER_DEBUG_ENABLE
                      printf("avg_voltage_mv == %u\n", avg_voltage_mv);
              #endif
 415   2      
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 8   

 416   2              percent = get_battery_percentage_by_voltage(avg_voltage_mv);
 417   2              // printf("percent == %u\n", (u16)percent);
 418   2      
 419   2              is_bat_vol_buff_get_avg_enable = 0;
 420   2          }
 421   1      
 422   1          if (bat_percent_update_time_is_comes())
 423   1          {
 424   2              bat_percent_update_time_reset();
 425   2      
 426   2              // 每次只变化1%的电池电量百分比
 427   2              if (bat_percent > percent && is_in_charging == 0)
 428   2              {
 429   3                  // 只有在放电时，才允许最后得到的电池电量百分比减少
 430   3                  if (bat_percent > 0)
 431   3                  {
 432   4                      bat_percent--;
 433   4                  }
 434   3              }
 435   2              else if (bat_percent < percent && is_in_charging == 1)
 436   2              {
 437   3                  // 只有在充电时，才允许最后得到的电池电量百分比增加
 438   3                  if (bat_percent < 100)
 439   3                  {
 440   4                      bat_percent++;
 441   4                  }
 442   3              }
 443   2      
 444   2      #if USER_DEBUG_ENABLE
                      printf("bat percent == %u\n", (u16)bat_percent);
                      printf("avg_voltage_mv == %u\n", avg_voltage_mv);
              #endif
 448   2          }
 449   1      
 450   1      #if USER_DEBUG_ENABLE
                  // printf("bat_percent = %u\n", (u16)bat_percent);
              
              #if 0
                  // @attention 只在测试时使用：
                  // 每隔一段时间打印一次滤波后得到的电压和电量百分比
                  if (flag_debug)
                  {
                      flag_debug = 0;
                      printf("max_voltage_mv == %u\n", max_voltage_mv);
                      printf("avg_voltage_mv == %u\n", avg_voltage_mv);
                      printf("bat_percent == %u\n", (u16)bat_percent); //
                  }
              #endif
              #endif
 465   1      
 466   1          // 控制电池电量指示灯
 467   1          if (is_in_charging &&
 468   1              (led_bat_level_sta != LED_BAT_LEVEL_STA_CHARGE_BEGIN &&
 469   1               led_bat_level_sta != LED_BAT_LEVEL_STA_CHARGE_BEGIN_ANIM &&
 470   1               led_bat_level_sta != LED_BAT_LEVEL_STA_CHARGING &&
 471   1               led_bat_level_sta != LED_BAT_LEVEL_STA_CHARGE_END))
 472   1          {
 473   2              led_bat_level_sta = LED_BAT_LEVEL_STA_CHARGE_BEGIN;
 474   2          }
 475   1          // 不在充电、蓝牙ic不工作、灯光关闭时，关闭电量指示灯
 476   1          else if (is_in_charging == 0 &&
 477   1                   ble_ic.is_working == 0 &&
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 9   

 478   1                   led_ctl.status == LED_STATUS_OFF &&
 479   1                   0 == is_in_discharging) // 充电ic没有输出放电的信号
 480   1          {
 481   2              led_bat_level_sta = LED_BAT_LEVEL_STA_IDLE;
 482   2      
 483   2              // 关机后（不在工作），需要在这里清空对应标志位
 484   2          }
 485   1          // 不在充电，但是蓝牙ic或者灯光打开，根据电池电量来点亮对应指示灯
 486   1          else if (is_in_charging == 0 &&
 487   1                   (ble_ic.is_working ||
 488   1                    led_ctl.status != LED_STATUS_OFF ||
 489   1                    is_in_discharging))
 490   1          {
 491   2              led_bat_level_sta = LED_BAT_LEVEL_STA_DISCHARGE;
 492   2      
 493   2              // 到了关机对应的电压
 494   2              if (avg_voltage_mv <= BATTERY_VOLTAGE_TURN_OFF_LOW_POWER)
 495   2              {
 496   3      #if USER_DEBUG_ENABLE
                          printf("detect bat power empty\n");
              #endif
 499   3      
 500   3                  if (0 == is_turn_off_by_low_bat)
 501   3                  {
 502   4                      // 关闭灯光
 503   4                      if (led_ctl.status != LED_STATUS_OFF)
 504   4                      {
 505   5                          led_status_set(LED_STATUS_OFF);
 506   5                      }
 507   4      
 508   4                      // 关闭蓝牙
 509   4                      if (ble_ic.is_working)
 510   4                      {
 511   5                          uart_data_send_cmd(UART_SEND_CMD_LOW_POWER_SHUTDOWN);
 512   5                          // delay_ms(5000); // 这里要等蓝牙播报完成
 513   5                          delay_ms(30);
 514   5                          ble_ic_disable_pre();
 515   5                          delay_ms(100);
 516   5                      }
 517   4      
 518   4                      // bat_percent = 0; // USER_TO_DO 可能需要加回来
 519   4                      is_turn_off_by_low_bat = 1;
 520   4                  }
 521   3              }
 522   2              // 到了 关闭蓝牙的阈值电压
 523   2              else if (avg_voltage_mv <= BATTERY_VOLTAGE_TURN_OFF_BLUETOOTH)
 524   2              {
 525   3                  // 关闭蓝牙
 526   3                  if (ble_ic.is_working)
 527   3                  {
 528   4                      uart_data_send_cmd(UART_SEND_CMD_LOW_POWER_SHUTDOWN);
 529   4                      // delay_ms(5000); // 这里要等蓝牙播报完成
 530   4                      delay_ms(30);
 531   4                      ble_ic_disable_pre();
 532   4                      delay_ms(100);
 533   4                  }
 534   3              }
 535   2              /*
 536   2                  低电量，并且没有发送过低电量报警，要打开蓝牙，发送低电量报警
 537   2              */
 538   2              else if (is_sent_low_bat_alert == 0 &&
 539   2                       avg_voltage_mv <= BATTERY_LOW_WARNING_VOLTAGE)
C51 COMPILER V9.60.7.0   BATTERY_MONITOR                                                   07/02/2026 15:05:08 PAGE 10  

 540   2              {
 541   3                  // u8 i; // 循环计数值，控制连续发送低电量报警的次数
 542   3      #if USER_DEBUG_ENABLE
                          // printf("detect low power\n");
              #endif
 545   3                  // 电池电量过低时，不打开低电量报警提示（不打开蓝牙）
 546   3                  if (battery_bluetooth_enable_is_allowed())
 547   3                  {
 548   4                      if (ble_ic.is_working == 0)
 549   4                      {
 550   5                          ble_ic_enable();
 551   5                      }
 552   4      
 553   4                      uart_data_send_cmd(UART_SEND_CMD_LOW_POWER_WARNING);
 554   4                      is_sent_low_bat_alert = 1;
 555   4                  }
 556   3              }
 557   2      
 558   2              // 灯开着，或者蓝牙正在工作，并且到了电池低电量状态，每隔一段时间�
             -�送一次低电量报警：
 559   2              if (is_send_low_bat_repeatedly_enable)
 560   2              {
 561   3                  uart_data_send_cmd(UART_SEND_CMD_LOW_POWER_WARNING);
 562   3                  is_send_low_bat_repeatedly_enable = 0;
 563   3      
 564   3      #if USER_DEBUG_ENABLE
                          printf("UART_SEND_CMD_LOW_POWER_WARNING\n");
              #endif
 567   3              }
 568   2          }
 569   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =   1243    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =     52      15
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
