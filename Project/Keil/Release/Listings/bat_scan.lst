C51 COMPILER V9.60.7.0   BAT_SCAN                                                          07/16/2026 18:47:24 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE BAT_SCAN
OBJECT MODULE PLACED IN .\Release\Objects\bat_scan.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\bat_scan.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECTOR(0X00
                    -0C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\driver_on_chip) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Release\Listi
                    -ngs\bat_scan.lst) OBJECT(.\Release\Objects\bat_scan.obj)

line level    source

   1          #include "bat_scan.h"
   2          #include "adc.h"
   3          #include "led.h"
   4          #include "charge_det.h"
   5          #include "battery_monitor.h"
   6          
   7          #include "user_config.h"
   8          
   9          // ====================================================================
  10          // static volatile u8 is_bat_vol_buff_add_enable = 0; // 是否允许往电池电压数组中放入数据
  11          
  12          // 控制一段时间内采集电池电压峰值（电压最大值）
  13          static volatile bat_vol_update_sta_t bat_vol_update_sta = BAT_VOL_UPDATE_STA_IDLE;
  14          static volatile u16 bat_vol_update_cnt = 0;
  15          
  16          // 记录一段时间内采集到的电压值：
  17          static volatile u16 bat_vol_history_buff[VOLTAGE_HISTORY_SIZE] = {0};
  18          static volatile u8 bat_vol_history_buff_idx = 0;
  19          // ====================================================================
  20          
  21          static volatile u16 max_voltage_mv = 0;    // 存放一段时间内采集到的最大电压值（只在采
             -集使用，不能作为最终的判断使用）
  22          static volatile u16 voltage_mv_global = 0; //
  23          
  24          // 采集电池电压的定时计数器
  25          static volatile u32 bat_avg_vol_scan_period_cnt = 0;
  26          // 最后得到的、稳定的电池电压
  27          volatile u16 avg_voltage_mv = 0; //
  28          
  29          void bat_get_avg_vol_period_add(void)
  30          {
  31   1              if (bat_avg_vol_scan_period_cnt < ((u32)-1))
  32   1              {
  33   2                      bat_avg_vol_scan_period_cnt++;
  34   2              }
  35   1      }
  36          
  37          // 由定时器调用，控制一段时间，获取电池电压的最大值（测试发现在充电和放�
             -��时，一段时间内电池电压的最大值是最接近电池电压实际值的）
  38          void bat_vol_update_timer_callback(void)
  39          {
  40   1              if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_IDLE)
  41   1              {
  42   2                      bat_vol_update_cnt = 0;
  43   2                      bat_vol_update_sta = BAT_VOL_UPDATE_STA_CAPTURING;
  44   2              }
  45   1              else if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_COMPLETED)
  46   1              {
  47   2                      // 已经采集完成，直接返回
  48   2                      return;
  49   2              }
  50   1      
  51   1              bat_vol_update_cnt++;
C51 COMPILER V9.60.7.0   BAT_SCAN                                                          07/16/2026 18:47:24 PAGE 2   

  52   1              if (bat_vol_update_cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD)
  53   1              {
  54   2                      /*
  55   2                              这里可以不用清零，只有 bat_vol_update_sta
  56   2                              刚进入 BAT_VOL_UPDATE_STA_IDLE 时清零
  57   2                      */
  58   2                      // bat_vol_update_cnt = 0;
  59   2                      bat_vol_update_sta = BAT_VOL_UPDATE_STA_COMPLETED;
  60   2              }
  61   1      }
  62          
  63          // 根据ad值，转换成对应的电池电压值(单位：mV)
  64          u16 get_battery_voltage_by_adc(u16 adc_val)
  65          {
  66   1              u16 voltage_mv = ADC_TO_BATTERY_VOLTAGE_MV(adc_val);
  67   1      
  68   1      #if 1
  69   1              // printf("voltage_mv == %u\n", voltage_mv);
  70   1              if (voltage_mv > 70)
  71   1              {
  72   2                      // 这里做电压补偿
  73   2                      voltage_mv -= 70;
  74   2              }
  75   1              // printf("voltage_mv == %u\n", voltage_mv);
  76   1      #endif
  77   1      
  78   1      #if 0
                  if (is_in_charging)
                  {
                      // 充电时，减去 0.1 V，作为补偿。如果电压接近 4.2V 时，不用补偿
                      if ((voltage_mv <= 4000) && voltage_mv > 100)
                      {
                          voltage_mv -= 100;
                      }
                  }
              #endif
  88   1      
  89   1              return voltage_mv;
  90   1      }
  91          
  92          void bat_vol_history_buff_init(u16 voltage_mv)
  93          {
  94   1              u8 i;
  95   1              for (i = 0; i < VOLTAGE_HISTORY_SIZE; i++)
  96   1              {
  97   2                      bat_vol_history_buff[i] = voltage_mv;
  98   2              }
  99   1              bat_vol_history_buff_idx = 0;
 100   1      }
 101          
 102          void bat_vol_history_buff_add(u16 voltage_mv)
 103          {
 104   1              bat_vol_history_buff[bat_vol_history_buff_idx] = voltage_mv;
 105   1              bat_vol_history_buff_idx = (bat_vol_history_buff_idx + 1) % VOLTAGE_HISTORY_SIZE;
 106   1              // bat_vol_history_buff_idx++;
 107   1              // if (bat_vol_history_buff_idx >= VOLTAGE_HISTORY_SIZE)
 108   1              // {
 109   1              //     bat_vol_history_buff_idx = 0;
 110   1              // }
 111   1      }
 112          
 113          // 获取缓冲区中所有元素的平均值
C51 COMPILER V9.60.7.0   BAT_SCAN                                                          07/16/2026 18:47:24 PAGE 3   

 114          u16 bat_vol_history_buff_get_avg(void)
 115          {
 116   1              u8 i;
 117   1              u32 ret = 0;
 118   1              for (i = 0; i < VOLTAGE_HISTORY_SIZE; i++)
 119   1              {
 120   2                      ret += bat_vol_history_buff[i];
 121   2              }
 122   1              return (u16)(ret / VOLTAGE_HISTORY_SIZE);
 123   1      }
 124          
 125          /**
 126           * @brief 更新电池电压
 127           *              由adc中断服务函数调用
 128           *              函数内部会更新最新得到的电池电压
 129           *
 130           */
 131          void battery_voltage_update_by_isr(void)
 132          {
 133   1              static volatile u8 is_initialized = 0;
 134   1              volatile u16 adc_val = 0;
 135   1              volatile u16 cur_voltage_mv = 0; // 存放当前采集到的电压值
 136   1      
 137   1              // 获取AD值（ad值有更新才获取）
 138   1              if (adc_get_update_flag(ADC_CHANNEL_SEL_BAT_DET))
 139   1              {
 140   2                      adc_clear_update_flag(ADC_CHANNEL_SEL_BAT_DET);
 141   2                      adc_val = adc_get_val(ADC_CHANNEL_SEL_BAT_DET);
 142   2                      // printf("bat adc val == %u\n", adc_val);
 143   2                      cur_voltage_mv = get_battery_voltage_by_adc(adc_val);
 144   2                      // printf("cur_voltage_mv == %u\n", cur_voltage_mv);
 145   2      
 146   2                      if (0 == is_initialized)
 147   2                      {
 148   3                              is_initialized = 1;
 149   3                              // 如果还没有采集过电池电压，直接将第一次采集到的ad值转换成电池电压
 150   3                              voltage_mv_global = cur_voltage_mv;
 151   3                              avg_voltage_mv = cur_voltage_mv;
 152   3      
 153   3                              bat_vol_history_buff_init(avg_voltage_mv);
 154   3      
 155   3                              // TODO 刚上电时，直接根据对应的电池电压，给放电时间进行赋值
 156   3                              // 试着直接赋值为向下一个挡位对应的放电时间？
 157   3                              // bat_charge_time_cnt_update(avg_voltage_mv);
 158   3                              // bat_discharge_time_cnt_update(avg_voltage_mv);
 159   3                              // led_bat_lev_sta_init(avg_voltage_mv);
 160   3      
 161   3      #if USER_DEBUG_ENABLE
 162   3                              printf("bat monitor init\n");
 163   3                              printf("avg_voltage_mv == %u\n", avg_voltage_mv);
 164   3      #endif
 165   3                      }
 166   2      
 167   2                      // 采集一段时间的电压值，取其中最大的值作为电池电压
 168   2                      /*
 169   2                              @attention
 170   2                              测试时，如果需要改变电池电压和电量百分比，需要屏蔽下面这段程序。
 171   2                              否则会更新 voltage_mv_global 和 max_voltage_mv，可能会被赋值为0，
 172   2                              导致又进入了 电池电量相关变量的初始化
 173   2                      */
 174   2                      /*
 175   2                              @attention 这里没有判断上一次的电压值，会实时更新 voltage_mv_global
C51 COMPILER V9.60.7.0   BAT_SCAN                                                          07/16/2026 18:47:24 PAGE 4   

 176   2                              在充电时，哪怕电压有下降，也会更新 voltage_mv_global
 177   2                              在放电时，电压有上升，也会更新 voltage_mv_global
 178   2                      */
 179   2                      if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_COMPLETED)
 180   2                      {
 181   3                              // 如果已经采集了一段时间的电压值，则将 voltage_mv_global 赋值为 max_voltage_mv
 182   3                              voltage_mv_global = max_voltage_mv;
 183   3      
 184   3                              max_voltage_mv = 0; // 清零，准备下一轮采集
 185   3                              bat_vol_update_sta = BAT_VOL_UPDATE_STA_IDLE;
 186   3      
 187   3                              // 输出计算结果 (调试用)
 188   3                              // printf("voltage_mv_global: %umV\n", voltage_mv_global);
 189   3                      }
 190   2                      else if (bat_vol_update_sta == BAT_VOL_UPDATE_STA_CAPTURING)
 191   2                      {
 192   3                              // 如果还在采集时间内，更新采集到的最大电压值 max_voltage_mv
 193   3                              if (max_voltage_mv < cur_voltage_mv)
 194   3                              {
 195   4                                      max_voltage_mv = cur_voltage_mv;
 196   4                              }
 197   3                      }
 198   2              }
 199   1      }
 200          
 201          /**
 202           * @brief 定期给存放电池电压的滑动平均数组添加数据
 203           *              由 1ms 定时器调用
 204           *
 205           */
 206          void bat_vol_history_buff_add_timer_callback(void)
 207          {
 208   1      
 209   1              static u32 cnt = 0;
 210   1              cnt++;
 211   1              if (cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD_IN_BUFFER)
 212   1              {
 213   2                      cnt = 0;
 214   2                      // 每隔一段时间，将采集到的电压值放入数组
 215   2                      bat_vol_history_buff_add(voltage_mv_global);
 216   2              }
 217   1      }
 218          
 219          void bat_scan(void)
 220          {
 221   1              if (voltage_mv_global == 0)
 222   1              {
 223   2                      return; // 尚未获取到电压值，不执行以下操作
 224   2              }
 225   1      
 226   1              // 每隔一段时间，将数组中的数据进行平均
 227   1              if (bat_avg_vol_scan_period_cnt >= BATTERY_VOLTAGE_UPDATE_PERIOD_IN_BUFFER_EXTRACT)
 228   1              {
 229   2                      bat_avg_vol_scan_period_cnt = 0;
 230   2                      avg_voltage_mv = bat_vol_history_buff_get_avg();
 231   2      #if USER_DEBUG_ENABLE
 232   2                      printf("avg_voltage_mv == %u\n", avg_voltage_mv);
 233   2      #endif
 234   2              }
 235   1      }


C51 COMPILER V9.60.7.0   BAT_SCAN                                                          07/16/2026 18:47:24 PAGE 5   

MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    603    ----
   CONSTANT SIZE    =     40    ----
   XDATA SIZE       =     39       9
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
