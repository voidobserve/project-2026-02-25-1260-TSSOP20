C51 COMPILER V9.60.7.0   UART0                                                             04/07/2026 18:03:41 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE UART0
OBJECT MODULE PLACED IN .\Release\Objects\uart0.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\driver_on_chip\uart0.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECT
                    -OR(0X000C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\driver_on_chip) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Releas
                    -e\Listings\uart0.lst) OBJECT(.\Release\Objects\uart0.obj)

line level    source

   1          #include "uart0.h"
   2          #include "user_config.h"
   3          
   4          volatile uart_rx_buffer_t uart0_rx_buffer = {0};
   5          
   6          #if USER_DEBUG_ENABLE
              // 重写puchar()函数
              char putchar(char c)
              {
                  while (!(UART0_STA & UART_TX_DONE(0x01)))
                      ;
                  UART0_DATA = c;
                  return c;
              }
              #endif
  16          
  17          void uart0_init(void)
  18          {
  19   1          P2_MD1 &= ~(GPIO_P25_MODE_SEL(0x3));
  20   1          P2_MD1 |= GPIO_P25_MODE_SEL(0x1); // 配置为输出模式
  21   1          FOUT_S25 |= GPIO_FOUT_UART0_TX;   // 配置为UART0_TX
  22   1      
  23   1          P2_PU |= GPIO_P26_PULL_UP(0x01);
  24   1          P2_MD1 &= ~GPIO_P26_MODE_SEL(0x03); // 清空对应的寄存器配置，对应输入模式
  25   1          FIN_S7 = GPIO_FIN_SEL_P26;          // 选择 uart0 rx 对应的引脚
  26   1      
  27   1          UART0_BAUD1 = (USER_UART0_BAUD >> 8) & 0xFF; // 配置波特率高八位
  28   1          UART0_BAUD0 = USER_UART0_BAUD & 0xFF;        // 配置波特率低八位
  29   1          UART0_CON0 = UART_STOP_BIT(0x00) |           // 0x00：一位停止位
  30   1                       UART_RX_IRQ_EN(0x01) |          // 0x01：rx中断使能
  31   1                       UART_EN(0x01);                  // UART 使能
  32   1      
  33   1          __SetIRQnIP(UART0_IRQn, 1); // 设置中断优先级，数值越大，优先级越高
  34   1          __EnableIRQ(UART0_IRQn);    // 打开模块中断
  35   1          IE_EA = 1;                  // 打开总中断
  36   1      }
  37          
  38          // UART0发送一个字节数据的函数
  39          void uart0_sendbyte(u8 byte)
  40          {
  41   1          while (!(UART0_STA & UART_TX_DONE(0x01))) // 等待上一次发送完成
  42   1              ;
  43   1          // IE_EA = 0; // 关闭总中断
  44   1          UART0_DATA = byte;
  45   1          // IE_EA = 1;
  46   1          // while (!(UART0_STA & UART_TX_DONE(0x01))) // 等待这次发送完成
  47   1          //     ;
  48   1      }
  49          
  50          void uart0_sendbuf(u8 *buf, u8 len)
  51          {
  52   1          u8 i = 0;
  53   1          for (; i < len; i++)
C51 COMPILER V9.60.7.0   UART0                                                             04/07/2026 18:03:41 PAGE 2   

  54   1          {
  55   2              uart0_sendbyte(buf[i]);
  56   2          }
  57   1      }
  58          
  59          // 获取接收缓冲区中有效的数据个数，单位：字节Byte
  60          u16 uart0_rxbuffer_get_count(void)
  61          {
  62   1          return uart0_rx_buffer.count;
  63   1      }
  64          
  65          // 从接收缓冲区中取出一个字节数据
  66          u8 uart0_rxbuffer_get_byte(void)
  67          {
  68   1          u8 rxbyte;
  69   1      
  70   1          if (0 == uart0_rx_buffer.count)
  71   1          {
  72   2              // 缓冲区空
  73   2              return 0;
  74   2          }
  75   1      
  76   1          // 先偏移索引，再取出数据
  77   1          // uart0_rx_buffer.tail = (uart0_rx_buffer.tail + 1) % UART_RX_BUF_SIZE;
  78   1          // rxbyte = uart0_rx_buffer.buffer[uart0_rx_buffer.tail];
  79   1      
  80   1          uart0_rx_buffer.tail++;
  81   1          if (uart0_rx_buffer.tail >= UART_RX_BUF_SIZE)
  82   1          {
  83   2              uart0_rx_buffer.tail = 0;
  84   2          }
  85   1          rxbyte = uart0_rx_buffer.buffer[uart0_rx_buffer.tail];
  86   1      
  87   1          uart0_rx_buffer.count--;
  88   1      
  89   1          return rxbyte;
  90   1      }
  91          
  92          static void uart0_rxbuffer_put_byte(u8 byte)
  93          {
  94   1          // 目前的逻辑：缓冲区满，覆盖旧的数据
  95   1      
  96   1          // 先偏移索引，再存放数据
  97   1          // uart0_rx_buffer.head = (uart0_rx_buffer.head + 1) % UART_RX_BUF_SIZE;
  98   1          // uart0_rx_buffer.buffer[uart0_rx_buffer.head] = byte;
  99   1      
 100   1          uart0_rx_buffer.head++;
 101   1          if (uart0_rx_buffer.head >= UART_RX_BUF_SIZE)
 102   1          {
 103   2              uart0_rx_buffer.head = 0;
 104   2          }
 105   1          uart0_rx_buffer.buffer[uart0_rx_buffer.head] = byte;
 106   1      
 107   1          uart0_rx_buffer.count++;
 108   1          if (uart0_rx_buffer.count > UART_RX_BUF_SIZE)
 109   1          {
 110   2              uart0_rx_buffer.count = UART_RX_BUF_SIZE;
 111   2          }
 112   1      }
 113          
 114          // UART0中断服务函数（接收中断）
 115          void UART0_IRQHandler(void) interrupt UART0_IRQn
C51 COMPILER V9.60.7.0   UART0                                                             04/07/2026 18:03:41 PAGE 3   

 116          {
 117   1          // 进入中断设置IP，不可删除
 118   1          __IRQnIPnPush(UART0_IRQn);
 119   1          // ---------------- 用户函数处理 -------------------
 120   1          // RX接收完成中断
 121   1          if (UART0_STA & UART_RX_DONE(0x1))
 122   1          {
 123   2              u8 recv_byte = UART0_DATA;
 124   2              uart0_rxbuffer_put_byte(recv_byte);
 125   2      
 126   2              // printf("%02x\n", (u16)recv_byte);
 127   2          }
 128   1          // 退出中断设置IP，不可删除
 129   1          __IRQnIPnPop(UART0_IRQn);
 130   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    294    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =    518       4
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
