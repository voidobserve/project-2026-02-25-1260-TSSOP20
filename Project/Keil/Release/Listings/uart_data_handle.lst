C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE UART_DATA_HANDLE
OBJECT MODULE PLACED IN .\Release\Objects\uart_data_handle.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\uart_data_handle.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVEC
                    -TOR(0X000C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\driver_on_chip) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Relea
                    -se\Listings\uart_data_handle.lst) OBJECT(.\Release\Objects\uart_data_handle.obj)

line level    source

   1          #include "uart_data_handle.h"
   2          #include "user_config.h"
   3          #include "uart0.h"
   4          #include <stdio.h>
   5          #include <string.h>
   6          
   7          #include "user_include.h"
   8          
   9          // 接收器
  10          static volatile uart_receiver_t uart_receiver;
  11          
  12          // 重置接收器状态
  13          void uart_receiver_reset(void)
  14          {
  15   1          // memset((void *)&uart_receiver, 0, sizeof(uart_receiver_t));
  16   1          uart_receiver.index = 0;
  17   1          uart_receiver.timeout_enable = 0;
  18   1          uart_receiver.timeout_cnt = 0;
  19   1          uart_receiver.status = UART_DATA_HANDLE_STATUS_IDLE;
  20   1      }
  21          
  22          /**
  23           * @brief 接收器超时使能时，累计超时时间，在定时器中断中调用
  24           *
  25           */
  26          void uart_receiver_timeout_add(void)
  27          {
  28   1          if (uart_receiver.timeout_enable)
  29   1          {
  30   2              uart_receiver.timeout_cnt++;
  31   2          }
  32   1      }
  33          
  34          /**
  35           * @brief 计算校验和。有接收完校验和之后，才调用该函数
  36           *
  37           * @return u8 0：校验通过；   1：校验不通过
  38           */
  39          u8 uart_receiver_process_checksum(void)
  40          {
  41   1          u8 i;
  42   1          u8 check_sum = 0;
  43   1          for (i = 0; i < 5; i++) // 只计算前5个字节的校验和
  44   1          {
  45   2              check_sum += uart_receiver.buffer[i];
  46   2          }
  47   1      
  48   1          if (check_sum == uart_receiver.buffer[6])
  49   1          {
  50   2              return 0;
  51   2          }
  52   1          else
  53   1          {
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 2   

  54   2      #if USER_DEBUG_ENABLE
                      // 校验和错误
                      printf("Checksum error: expected 0x%02x, got 0x%02x\n",
                             (u16)check_sum, (u16)uart_receiver.buffer[6]);
              #endif
  59   2      
  60   2              return 1;
  61   2          }
  62   1      }
  63          
  64          /**
  65           * @brief 接收器处理单个字节
  66           *
  67           * @param byte
  68           * @return u8 0：接收成功
  69           *            非0：接收失败
  70           */
  71          u8 uart_receiver_process_byte(u8 byte)
  72          {
  73   1          // printf("%02x ", (u16)byte);
  74   1          switch (uart_receiver.status)
  75   1          {
  76   2          case UART_DATA_HANDLE_STATUS_IDLE:
  77   2              if (byte == UART_DATA_HANDLE_FORMAT_HEAD0)
  78   2              {
  79   3                  uart_receiver.buffer[uart_receiver.index++] = byte;
  80   3                  uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_HEAD0;
  81   3                  return 0;
  82   3              }
  83   2              else
  84   2              {
  85   3                  return 1;
  86   3              }
  87   2              break;
  88   2      
  89   2          case UART_DATA_HANDLE_STATUS_FORMAT_HEAD0:
  90   2              if (byte == UART_DATA_HANDLE_FORMAT_HEAD1)
  91   2              {
  92   3                  uart_receiver.buffer[uart_receiver.index++] = byte;
  93   3                  uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_HEAD1;
  94   3                  return 0;
  95   3              }
  96   2              else
  97   2              {
  98   3                  // 错误
  99   3                  return 1;
 100   3              }
 101   2              break;
 102   2      
 103   2          case UART_DATA_HANDLE_STATUS_FORMAT_HEAD1:
 104   2              if (byte == UART_DATA_HANDLE_FORMAT_FIX_VAL0)
 105   2              {
 106   3                  uart_receiver.buffer[uart_receiver.index++] = byte;
 107   3                  uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_FIX_VAL0;
 108   3                  return 0;
 109   3              }
 110   2              else
 111   2              {
 112   3                  return 1;
 113   3              }
 114   2              break;
 115   2      
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 3   

 116   2          case UART_DATA_HANDLE_STATUS_FORMAT_FIX_VAL0:
 117   2              // CMD字节，任何值都可以接受
 118   2              uart_receiver.buffer[uart_receiver.index++] = byte;
 119   2              uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_CMD;
 120   2              return 0;
 121   2              break;
 122   2      
 123   2          case UART_DATA_HANDLE_STATUS_FORMAT_CMD:
 124   2              // DATA字节，任何值都可以接受
 125   2              uart_receiver.buffer[uart_receiver.index++] = byte;
 126   2              uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_DATA;
 127   2              return 0;
 128   2              break;
 129   2      
 130   2          case UART_DATA_HANDLE_STATUS_FORMAT_DATA:
 131   2              if (byte == UART_DATA_HANDLE_FORMAT_FIX_VAL1)
 132   2              {
 133   3                  uart_receiver.buffer[uart_receiver.index++] = byte;
 134   3                  uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_FIX_VAL1;
 135   3                  return 0;
 136   3              }
 137   2              else
 138   2              {
 139   3                  return 1;
 140   3              }
 141   2              break;
 142   2      
 143   2          case UART_DATA_HANDLE_STATUS_FORMAT_FIX_VAL1:
 144   2              // 校验和字节
 145   2              uart_receiver.buffer[uart_receiver.index++] = byte;
 146   2              uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_CHECK_SUM;
 147   2              if (uart_receiver_process_checksum())
 148   2              {
 149   3                  // 校验和出错
 150   3                  return 1;
 151   3              }
 152   2              else
 153   2              {
 154   3                  return 0;
 155   3              }
 156   2              break;
 157   2      
 158   2          case UART_DATA_HANDLE_STATUS_FORMAT_CHECK_SUM:
 159   2              if (byte == UART_DATA_HANDLE_FORMAT_TAIL)
 160   2              {
 161   3                  uart_receiver.buffer[uart_receiver.index++] = byte;
 162   3                  uart_receiver.status = UART_DATA_HANDLE_STATUS_FORMAT_TAIL;
 163   3                  return 0;
 164   3              }
 165   2              else
 166   2              {
 167   3                  return 1;
 168   3              }
 169   2              break;
 170   2      
 171   2          default:
 172   2              break;
 173   2          }
 174   1      
 175   1          return 1;
 176   1      }
 177          
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 4   

 178          // 超时处理函数
 179          void uart_receiver_timeout_handler(void)
 180          {
 181   1      #if USER_DEBUG_ENABLE
                  u8 i;
              #endif
 184   1      
 185   1          if (uart_receiver.timeout_cnt >= UART_DATA_HANDLE_TIMEOUT)
 186   1          {
 187   2      #if USER_DEBUG_ENABLE
                      printf("UART receive timeout, current status: %d\n", (u16)uart_receiver.status);
              
                      // 打印当前缓冲区内容
                      printf("Buffer content: \n");
                      for (i = 0; i < uart_receiver.index; i++)
                      {
                          printf("0x%02x ", (u16)uart_receiver.buffer[i]);
                      }
                      printf("\n");
              #endif
 198   2      
 199   2              // 重置接收器
 200   2              uart_receiver_reset();
 201   2          }
 202   1      }
 203          void uart_data_handle(void)
 204          {
 205   1      #if USER_DEBUG_ENABLE
                  u8 i;
              #endif
 208   1          u8 recv_byte;
 209   1          static u8 initialized = 0; // 串口接收器对象是否已经完成初始化
 210   1      
 211   1          // 初始化接收器（只执行一次）
 212   1          if (!initialized)
 213   1          {
 214   2              uart_receiver_reset();
 215   2              initialized = 1;
 216   2          }
 217   1      
 218   1          // 检查超时
 219   1          // if (uart_receiver.timeout_enable &&
 220   1          //     uart0_rxbuffer_get_count() == 0)
 221   1          if (uart_receiver.timeout_enable)
 222   1          {
 223   2              uart_receiver_timeout_handler();
 224   2          }
 225   1      
 226   1          // if (uart0_rxbuffer_get_count() == 0)
 227   1          // {
 228   1          //     return;
 229   1          // }
 230   1      
 231   1      #if 1
 232   1          // 处理接收缓冲区中的数据
 233   1          while (1)
 234   1          {
 235   2              if (0 == uart0_rxbuffer_get_count() ||
 236   2                  uart_receiver.status == UART_DATA_HANDLE_STATUS_FORMAT_TAIL ||
 237   2                  uart_receiver.status == UART_DATA_HANDLE_STATUS_FORMAT_CHECK_SUM) // 接收完校验和就认�
             -��接收完成
 238   2              {
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 5   

 239   3      #if USER_DEBUG_ENABLE
                          // 缓冲区为空或者接收完成，退出循环
                          // if (0 == uart0_rxbuffer_get_count() && uart_receiver.status != UART_DATA_HANDLE_STATUS_FORM
             -AT_TAIL)
                          // {
                          //     printf("0 == uart0_rxbuffer_get_count()\n");
                          // }
              
                          // if (uart_receiver.status == UART_DATA_HANDLE_STATUS_FORMAT_TAIL)
                          // {
                          //     printf("recv complete \n");
                          // }
              
              #endif
 252   3                  break;
 253   3              }
 254   2      
 255   2              recv_byte = uart0_rxbuffer_get_byte();
 256   2      
 257   2              // 启用超时计数
 258   2              uart_receiver.timeout_enable = 1;
 259   2              uart_receiver.timeout_cnt = 0;
 260   2      
 261   2              // 处理字节
 262   2              if (uart_receiver_process_byte(recv_byte))
 263   2              {
 264   3      #if USER_DEBUG_ENABLE
                          // 处理失败
                          // 如果连格式头都没有接收，不打印错误信息
                          if (uart_receiver.status != 0)
                          {
                              printf("Byte processing failed\n");
                              printf("cur uart receiver status: %02d\n", (u16)uart_receiver.status);
                          }
              
                          // printf("cur uart receiver index: %02d\n", (u16)uart_receiver.index);
                          // printf("cur recved byte: 0x%02x\n", (u16)recv_byte);
              #endif
 276   3                  uart_receiver_reset();
 277   3              }
 278   2          }
 279   1      #else
                  if (uart0_rxbuffer_get_count())
                  {
                      recv_byte = uart0_rxbuffer_get_byte();
              
                      // 启用超时计数
                      uart_receiver.timeout_enable = 1;
                      uart_receiver.timeout_cnt = 0;
              
                      // 处理字节
                      if (uart_receiver_process_byte(recv_byte))
                      {
              #if USER_DEBUG_ENABLE
                          // 处理失败
                          // 如果连格式头都没有接收，不打印错误信息
                          if (uart_receiver.status != 0)
                          {
                              printf("Byte processing failed\n");
                              printf("cur uart receiver status: %02d\n", (u16)uart_receiver.status);
                          }
              
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 6   

                          // printf("cur uart receiver index: %02d\n", (u16)uart_receiver.index);
                          // printf("cur recved byte: 0x%02x\n", (u16)recv_byte);
              #endif
                          uart_receiver_reset();
                      }
                  }
              #endif
 307   1      
 308   1          if (uart_receiver.status != UART_DATA_HANDLE_STATUS_FORMAT_TAIL &&
 309   1              uart_receiver.status != UART_DATA_HANDLE_STATUS_FORMAT_CHECK_SUM) // 接收完校验和就认为�
             -�收完成
 310   1          {
 311   2              return;
 312   2          }
 313   1      
 314   1          // ===========================================================
 315   1          // 接收完成，处理数据
 316   1      
 317   1      #if USER_DEBUG_ENABLE
              #if 0
                  // 打印接收到的一帧数据
                  printf("================================>\n");
                  printf("Received complete frame: \n");
                  for (i = 0; i < uart_receiver.index; i++)
                  {
                      printf("0x%02x ", (u16)uart_receiver.buffer[i]);
                  }
                  printf("\n");
                  printf("================================^\n");
              #endif
              #endif
 330   1      
 331   1          //
 332   1          switch ((uart_recv_cmd_t)uart_receiver.buffer[4])
 333   1          {
 334   2          case UART_RECV_CMD_BLE_MUSIC_PLAYING:
 335   2              // printf("recv music playing\n");
 336   2              ble_ic.music_status = BLUETOOTH_IC_STATUS_PLAYING_MUSIC;
 337   2              break;
 338   2          case UART_RECV_CMD_BLE_MUSIC_PAUSING:
 339   2              // printf("recv music pause\n");
 340   2              ble_ic.music_status = BLUETOOTH_IC_STATUS_PAUSE_MUSIC;
 341   2              break;
 342   2          case UART_RECV_CMD_BLE_AMP_OFF:
 343   2      #if USER_DEBUG_ENABLE
                      printf("recv amp off\n");
              #endif
 346   2              // 收到蓝牙ic回复的数据后，关闭蓝牙ic
 347   2              // BLE_IC_POWER_KEY_PIN = 1;
 348   2              // ble_ic.is_working = 0;
 349   2      
 350   2              ble_ic_disable_post();
 351   2              break;
 352   2      
 353   2          default:
 354   2      #if USER_DEBUG_ENABLE
                      printf("Unknown command: 0x%02x\n", (u16)uart_receiver.buffer[4]);
              #endif
 357   2              break;
 358   2          }
 359   1      
 360   1          // 重置接收器，准备下一次接收
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  07/29/2026 17:46:38 PAGE 7   

 361   1          uart_receiver_reset();
 362   1      }
 363          
 364          // 根据命令，自动打包数据并发送
 365          // 串口数据发送
 366          void uart_data_send_cmd(uart_send_cmd_t cmd)
 367          {
 368   1          u8 buf[] = {
 369   1              UART_DATA_HANDLE_FORMAT_HEAD0,
 370   1              UART_DATA_HANDLE_FORMAT_HEAD1,
 371   1              UART_DATA_HANDLE_FORMAT_FIX_VAL0,
 372   1              0x80,
 373   1              0xFF, // cmd
 374   1              UART_DATA_HANDLE_FORMAT_FIX_VAL1,
 375   1              0xFF, // check_sum
 376   1              UART_DATA_HANDLE_FORMAT_TAIL,
 377   1          };
 378   1      
 379   1          buf[4] = cmd;                                        // cmd
 380   1          buf[6] = buf[0] + buf[1] + buf[2] + buf[3] + buf[4]; // check_sum
 381   1      
 382   1          uart0_sendbuf(buf, ARRAY_SIZE(buf));
 383   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    464    ----
   CONSTANT SIZE    =      8    ----
   XDATA SIZE       =     26       9
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
